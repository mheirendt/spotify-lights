export default class MediaService {

    static async info() {
        return Promise.resolve(false);
    }

    static async analyis(_id) { /* eslint-disable-line */
        return Promise.resolve(false);
    }

    static async play() {
        return Promise.resolve(false);
    }

    static async pause() {
        return Promise.resolve(false);
    }

    static async previous() {
        return Promise.resolve(false);
    }

    static async next() {
        return Promise.resolve(false);
    }

    static async seek(_progress) { /* eslint-disable-line */
        return Promise.resolve(false);
    }

    static async shuffle(_turnOn) { /* eslint-disable-line */
        return Promise.resolve(false);
    }

    static async repeat(_turnOn) { /* eslint-disable-line */
        return Promise.resolve(false);
    }
}